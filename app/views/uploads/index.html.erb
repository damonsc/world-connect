<% if !flash[:notice].blank? %>
   <div class = "alert alert-info">
      <%= flash[:notice] %>
   </div>
<% end %>
<!------check to see if content code is empty------>
<% if user_signed_in? %>
<% if current_user.content_code == nil && current_user.pc_client %>
<div class="alert alert-danger" role="alert">
please contact and inform us that you are not connected to our upload server</div>

<% end %>
<% end %>
<!------------------------------------------------->
<br />

<%= link_to "New Uploads", uploads_new_path, class: "btn btn-primary" %>
<br />
<br />

<table class = "table table-borderless">
   <thead>
      <tr>
         <th>Name</th>
         <th>Download Link</th>
         <th>Who's this document for</th>
		  <% if current_user.admin? %>
		  <th>refererence code </th>
		  <% end %>
      </tr>
   </thead>
 
   <tbody>
	    
      <% @upload.each do |upload| %>
         
         <tr>
            <td><%= upload.name %></td>
            <td><%= link_to "Download file", upload.attachment_url %></td>
			 <td><%= upload.intended_user %></td>
			 <% if current_user.admin? %>
			  <td><%= upload.reference_code %></td>
			 
            <td><%= button_to "Delete",  upload, method: :delete, class: "btn btn-danger", confirm: "Are you sure that you wish to delete #{upload.name}?" %></td>
			<% end %>
         </tr>
         
      <% end %>
	   
	   
	   <% if user_signed_in? && current_user.content_code != nil%>
	   
	   <% @upload2.each do |upload| %>
         
         <tr>
            <td class="table-info"><%= upload.name %></td>
            <td class="table-info"><%= link_to "Download file", upload.attachment_url %></td>
			 <td class="table-info"><%= upload.intended_user %></td>
			 <% if current_user.admin? %>
			  <td class="table-info"><%= upload.reference_code %></td>
            <td><%= button_to "Delete",  upload, method: :delete, class: "btn btn-danger", confirm: "Are you sure that you wish to delete #{upload.name}?" %></td>
			<% end %>
         </tr>
         
      <% end %>
	   <% end %>

	  
   </tbody>
	
	
	
	

<%= link_to "Home", root_path, class: "btn btn-primary" %>

