<p id="notice"><%= notice %></p>
<%= render 'home/header' %>
<h1>Check the status of your requests</h1>
<br>
<ul class="list-inline">
		
	<li class="list-inline-item"><%= link_to 'Pending Tasks', status_tasks_path, class:'btn btn-light' %></li>
	<li class="list-inline-item"><%= link_to 'Completed Tasks', completed_tasks_path, class:'btn btn-light' %></li>
	
</ul>

<table class="table">
  <thead>
    <tr>
		<th>The request</th>
      <th>The latest update</th>
      <th>in progress</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
	  <% if task.complete == false %>
      <% if task.code = current_user.connect_code && task.client_code = current_user.content_code%>
	  <tr>
		   <td><%= task.description %></td>	
        <td><%= task.status %></td>	
		  <td><%= task.code %></td>	
		  <td><%= task.client_code %></td>	
		
        <td><span class="glyphicon glyphicon-refresh alert alert-alert"></span></td>
          
		  
      </tr>
	  <% end %>
	  <% end %>
    <% end %>
  </tbody>
</table>